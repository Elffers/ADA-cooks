<!DOCTYPE html>
<html>
<head>
  <title>ADACooks</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %> 
  <link href='http://fonts.googleapis.com/css?family=Oleo+Script+Swash+Caps' rel='stylesheet' type='text/css'>
  <!-- can add style sheet here just as "sheet" , with actual .css file stored in app/assets/stylesheets folder, but the default layout is is scaffolds.css.scss-->
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div id="nav-bar">
      <h1 id="title-header">ADA Cooks</h1>
      <%= link_to 'Recipes', recipes_path %>
      <%= link_to 'Cookbook', cookbooks_path %>
     <div id="search-bar">
      <div id="recipe_search">
        <%= form_tag("/search", method: "get") do %>
          <%= label_tag(:name, "Find recipe by name!") %>
          <%= text_field_tag(:name, params[:name]) %>
          <%= submit_tag("Search") %>
        <% end %>
      </div>
        <div id="ingredient_search">
          <%= form_tag("/search_by_ingredient", method: "get") do %>
            <%= label_tag(:ingredient, "Find recipe by ingredient!") %>
            <%= select_tag(:ingredient, options_for_select(Ingredient.all)) %>
            <%= submit_tag("Search") %>
          <% end %>
        </div>
     </div>
   </div>
  </header>

<%= yield %>

</body>
</html>
